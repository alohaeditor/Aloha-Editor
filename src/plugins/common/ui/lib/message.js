define([
	"jquery",
	"ui/component"
],
function( jQuery, Component ) {

	return {
		'confirm': function(props) {
			//props.title
			//props.text
			//props.yes function
			//props.no function
			//maybe props.close function
		},
		'alert': function(props) {
			//props.title
			//props.text
			//maybe props.close fuction 
		}
	};
});
