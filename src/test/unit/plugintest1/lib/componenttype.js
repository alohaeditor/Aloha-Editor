define(
[], // no dependency
function() {
    "use strict";
 
    var componenttype = Class.extend({
        doSome: function() {
            // huhu
        }
    });
 
    return componenttype;
 
});