(function(window, undefined) {

	Aloha = window.Aloha || {};

	Aloha.settings = {
		locale: 'en',
		plugins: {
			format: {
				config: [  'b', 'i', 'p', 'sub', 'sup', 'del', 'title', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'pre', 'removeFormat' ]
			}
		},
		sidebar: {
			disabled: true
		}
	};

})(window);